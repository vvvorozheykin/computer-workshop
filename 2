#include<iostream>
#include<cmath>
using namespace std;
int main (){
    int k;
   cout << "vvedite k:" ;
   cin >> k;
   int number = ceil(k/2.0);
   if ( k % 2 ==0 ){
       cout << "nomer pari chisel:" << number << endl;
   } else {
       cout << "nomer pari chisel:" << (number + 1) << endl;
   }
   }
